#include "CreateOutput.h"
#include <stdio.h>

void CreateOutput(ParsedFile* parsedFile) {
    // Implement object file creation logic here
    printf("Creating object file for: %s\n", parsedFile->fileName);
    // TODO: Create .ext, .ent, .ob files.
}